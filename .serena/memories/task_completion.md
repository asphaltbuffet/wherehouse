# Task Completion Checklist

1. `mise run lint` — must be clean (0 issues)
2. `mise run test` — all tests pass
3. `/pre-commit` skill before every commit
4. `/commit` skill for message (conventional commits: feat/fix/chore/docs/refactor/test)
5. `/audit-docs` after features or fixes
6. Deferred work → GitHub Issues at `github.com/asphaltbuffet/wherehouse`, not TODOs in code
