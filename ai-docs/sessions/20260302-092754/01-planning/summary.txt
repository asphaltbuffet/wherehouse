Replace github.com/google/uuid with github.com/matoous/go-nanoid/v2 via a new internal/nanoid wrapper package, update the two ID-generation sites (cmd/add/item.go and cmd/add/location.go), replace LooksLikeUUID with a nanoid-aware LooksLikeID detector in internal/cli/selectors.go, and remap all 14 test constants to 10-character fixed strings. The database migration requires a Go-assisted command (wherehouse db migrate-ids) rather than a pure SQL file because the events table stores entity IDs embedded in JSON payloads that must be string-replaced atomically alongside projection table updates. System location IDs (Missing, Borrowed, Loaned) will be remapped from their current UUID constants to fixed 10-character deterministic nanoid-compatible strings (sys0000001/sys0000002/sys0000003) in migration 003.
