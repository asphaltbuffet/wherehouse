# Task A Changes

## Files Created

### internal/database/eventtype_string.go (generated)
Generated by `stringer -type=EventType -linecomment` via `mise run generate`. Provides `String()` method on `EventType` that returns the linecomment annotation (e.g., `"item.created"`). Do not edit manually.

### internal/database/eventTypes_sql.go (new)
Implements `driver.Valuer` and `sql.Scanner` for `EventType`:
- `Value() (driver.Value, error)` — returns `et.String()` so the typed int is persisted as a string
- `Scan(src any) error` — uses `ParseEventType` to convert a database string back to the typed constant

### internal/database/eventTypes_test.go (new)
TDD tests written before implementation (Step A1):
- `TestEventTypeString` — asserts all 15 constants have correct string representations
- `TestParseEventType` — round-trips every constant through `ParseEventType(et.String()) == et`
- `TestParseEventTypeUnknown` — asserts error returned for unknown strings

### internal/database/eventTypes_sql_test.go (new)
TDD tests written before implementation (Step A1):
- `TestEventTypeValuer` — asserts `Value()` returns the correct string
- `TestEventTypeScanner` — asserts `Scan()` parses strings correctly and errors on invalid input

## Files Modified

### internal/database/eventTypes.go
- Added `import "fmt"`
- Added `//go:generate stringer -type=EventType -linecomment` directive
- Changed `ItemCreatedEvent = iota + 1` to `ItemCreatedEvent EventType = iota + 1` to make stringer recognize typed constants
- Added `var eventTypeByName` map initialized via `.String()` calls (not string literals)
- Added `ParseEventType(s string) (EventType, error)` function using map lookup

### internal/database/events.go
- Changed `Event.EventType` field type from `string` to `EventType`
- Changed `AppendEvent` signature: `eventType string` → `eventType EventType`
- Changed `GetEventsByType` signature: `eventType string` → `eventType EventType`
- `driver.Valuer` implementation means `eventType` passes correctly to SQL driver as string

### internal/database/eventHandler.go
- Replaced all 15 string case literals in `processEventInTx` switch with typed constants:
  - `"location.created"` → `LocationCreatedEvent`
  - `"location.renamed"` → `LocationRenamedEvent`
  - `"location.reparented"` → `LocationMovedEvent`
  - `"location.deleted"` → `LocationDeletedEvent`
  - `"item.created"` → `ItemCreatedEvent`
  - `"item.moved"` → `ItemMovedEvent`
  - `"item.missing"` → `ItemMissingEvent`
  - `"item.borrowed"` → `ItemBorrowedEvent`
  - `"item.loaned"` → `ItemLoanedEvent`
  - `"item.found"` → `ItemFoundEvent`
  - `"item.deleted"` → `ItemDeletedEvent`
  - `"project.created"` → `ProjectCreatedEvent`
  - `"project.completed"` → `ProjectCompletedEvent`
  - `"project.reopened"` → `ProjectReopenedEvent`
  - `"project.deleted"` → `ProjectDeletedEvent`

### internal/database/helper_test.go
- Changed `insertEvent` signature: `eventType string` → `eventType EventType`
- Replaced all string literal event types in `SeedTestData` with typed constants

### internal/database/integration_test.go
- Replaced all `insertEvent` string literal calls with typed constants
- Replaced `GetEventsByType` string literal calls with typed constants
- Replaced `evt.EventType` string comparison with typed constant comparison

### internal/database/search_test.go
- Replaced 3 `insertEvent` string literal calls with typed constants (`LocationCreatedEvent`, `ItemCreatedEvent`, `ItemMovedEvent`)

## Notes

- Database schema unchanged — string storage preserved
- Batch B (golang-developer, golang-ui-developer) still needed for `internal/cli/` and `cmd/` call sites
- All `internal/database/` tests pass: `go test ./internal/database/...` → ok
